# 2026-05-21 工作记录

## 完成事项

### 1. 代码理解
- 完整阅读 KNN-MMD 仓库全部源码（model.py, dataset.py, func.py, train.py, train_fall.py）
- 理解跨域 WiFi CSI HAR 的完整 pipeline：UMAP降维 → KNN伪标签 → ResNet+MK-MMD域对齐
- 读懂 WiFall 数据集结构：10人×5动作，928个CSV，ESP32-S3采集，52子载波

### 2. 环境配置
- myenv 环境补装 `umap-learn`
- 确认 torch 2.2.2+cu118、scikit-learn 1.8.0 等依赖版本正常

### 3. 数据预处理
- 修复 `process1.py` 的 subcarrier 索引 bug（`x[i*2-1]` → `x[i*2+1]`）
- 修复 `process1.py` 的数据路径（`./data` → `../WiFall`）
- 完成 CSV → pkl → npy 全流程，生成 1857 个样本
- 数据存放于 `data/fall/`，shape: `(1857, 100, 52)`，每样本 1 秒 100 帧

### 4. Bug 修复
- **UMAP spectral 维度限制**：目标域样本少（64个）时 `n_components=128` 会报错 `k >= N`，在 `dimension_reducation()` 中加了 `min(output_dim, n_samples - 2)` 保护（train_fall.py + train.py）
- **训练 NaN 问题**：MK-MMD 损失在 epoch 181 时产生 NaN，导致模型参数被污染 + Dashboard JSON 解析失败。三重修复：
  - `iteration()` 中 NaN/Inf 检测，跳过异常 batch 的 optimizer step
  - metrics.json 序列化前 NaN/Inf → null
  - best_info.json 同理

### 5. 新功能开发
- **训练日志系统**：每次训练自动创建 `logs/run_{时间戳}_{task}/`，包含 args.json、metrics.json、best_info.json、best_model.pth、best_cls.pth
- **Web Dashboard**：`dashboard.py`，纯标准库 HTTP 服务器 + Chart.js 前端，实时查看 acc/loss 曲线，支持切换不同 run，5 秒自动刷新

### 6. 其他
- 创建 `.gitignore`（忽略 *.npy, *.pkl, *.pth, *.txt, __pycache__/）
- 推送代码至 GitHub（大文件已排除）

## 论文实验计划

根据论文 Table II / III，需要跑的 WiFall 实验：

### 实验 1：跨域动作识别（Action Recognition）
- 协议：Leave-one-person-out，10 折
- 参数：`--task action --k 5 --n 3 --p 0.5 --lr 0.0005 --mode 1`
- 论文预期：**KNN-MMD WiFall K=5 ≈ 78%**
- 状态：待开始

### 实验 2：跨域人员识别（People Identification）
- 协议：Leave-one-action-out，5 折
- 参数：`--task people --k 5 --n 3 --p 0.5 --lr 0.001 --mode 1`
- 论文预期：**KNN-MMD WiFall K=5 ≈ 73%**
- 状态：待开始

### 实验 3：消融实验
- K 值影响（1,2,3,5,7）
- p 值影响（0.1,0.3,0.5,0.7,0.9）
- 状态：待开始

## 明天起点

1. 启动实验 1（10折动作识别），跑完全部 10 折
2. 汇总 10 折结果，与论文 Table II 对比
3. 如果时间允许，启动实验 2（人员识别）
